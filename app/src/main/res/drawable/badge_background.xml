<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Colore di sfondo del badge -->
    <solid android:color="#FF0000" />

    <!-- Dimensioni minime del badge -->
    <size
        android:width="24dp"
        android:height="24dp"/>

    <!-- Padding interno per il testo -->
    <padding
        android:left="6dp"
        android:right="6dp"
        android:top="2dp"
        android:bottom="2dp"/>
</shape>